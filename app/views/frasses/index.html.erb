<h1>Frase一覧</h1><p>※fraseはイタリア語で文章という意味です</p>


<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

  <% @frasses.each do |frasse| %>
  <tr>
    <td><%= frasse.content %></td>

    <td><%= link_to '詳細',frass_path(frasse.id) %></td>
    <td><%= link_to "編集", edit_frass_path(frasse.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to 'new Frase!!', new_frass_path %>